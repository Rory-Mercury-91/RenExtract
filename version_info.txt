# VSVersionInfo file for PyInstaller
# This file adds version metadata to the .exe which helps reduce false positives
# Format compatible with PyInstaller sous Windows et Wine

VSVersionInfo(
  ffi=FixedFileInfo(
    # filevers and prodvers should be always a tuple with four items: (1, 2, 3, 4)
    # Set not needed items to zero 0.
    filevers=(2025, 10, 20, 1),
    prodvers=(2025, 10, 20, 1),
    # Contains a bitmask that specifies the valid bits 'flags'r
    mask=0x3f,
    # Contains a bitmask that specifies the Boolean attributes of the file.
    flags=0x0,
    # The operating system for which this file was designed.
    # 0x4 - NT and there is no need to change it.
    OS=0x40004,
    # The general type of file.
    # 0x1 - the file is an application.
    fileType=0x1,
    # The function of the file.
    # 0x0 - the function is not defined for this fileType
    subtype=0x0,
    # Creation date and time stamp.
    date=(0, 0)
    ),
  kids=[
    StringFileInfo(
      [
      StringTable(
        u'040904B0',
        [StringStruct(u'CompanyName', u'RenExtract Open Source Project'),
        StringStruct(u'FileDescription', u'RenExtract - Extraction et traduction pour Ren\'Py'),
        StringStruct(u'FileVersion', u'2025.10.20.1'),
        StringStruct(u'InternalName', u'RenExtract'),
        StringStruct(u'LegalCopyright', u'MIT License - Copyright (c) 2025 RenExtract Contributors'),
        StringStruct(u'OriginalFilename', u'RenExtract.exe'),
        StringStruct(u'ProductName', u'RenExtract'),
        StringStruct(u'ProductVersion', u'2025.10.20.1'),
        StringStruct(u'Comments', u'Professional translation tool for Ren\'Py visual novels - Open Source'),
        StringStruct(u'LegalTrademarks', u'')])
      ]), 
    VarFileInfo([VarStruct(u'Translation', [1033, 1200])])
  ]
)

